# create-qing

CLI for generating a complete browser-only React 19 + TanStack Router + Ant Design 6 administration project, plus repository-aware pages.

```sh
pnpm dlx create-qing create my-admin
cd my-admin
pnpm dev
```

The generated project includes its own `AGENTS.md` and three validated skills under `.agents/skills`.
